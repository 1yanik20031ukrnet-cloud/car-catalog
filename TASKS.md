# Задачи проекта

Роли (можно менять по договорённости):

- **Ян** — backend: модели, база, фильтры, админка.
- **Друг** — frontend: шаблоны, CSS, карточки, мобильная версия.

Статусы: `[ ]` не начато, `[~]` в работе, `[x]` готово (замёржено в main).

## Этап 0 — каркас

- [x] Каркас Django-проекта, Git, документация — *Ян* (прямо в `main`,
  это единственное исключение)

## Этап 1 — данные и первый экран (можно делать параллельно)

- [x] **Модели и админка** — *Ян*, ветка `feature/car-models`
  - модели `Dealer`, `Car`, `CarImage`, `Booking` + миграции;
  - регистрация в админке (таблица: фото, автомобиль, цена, статус);
  - демо-данные: 12–16 автомобилей (BMW, Audi, Mercedes, VW, Porsche,
    Volvo) командой `python manage.py seed_demo`;
  - файлы: `cars/models.py`, `cars/admin.py`, `cars/management/`.
- [x] **Вёрстка каталога** — *Друг*, ветка `feature/catalog-ui`
  (замёржена 2026-09-03) — шапка с контактами, карточки с
  мини-каруселью фото, цена и статус поверх фото, «Сначала новые».
  `catalog()` теперь берёт настоящие машины из базы (было — пустая
  заглушка) — Ян доклеил `is_published=True` в фильтр при слиянии.
  Заодно завёл `cars/formatting.py` (форматирование цены/пробега,
  русские склонения «1 машина / 3 машины / 16 машин») и
  `cars/templatetags/car_extras.py` — общие для каталога, страницы
  машины и (по плану) админки.

- [x] **Удобная загрузка фото и внешний вид админки** — *Ян*, ветка
  `feature/admin-usability`
  - одно поле, в котором можно выбрать сразу несколько фотографий;
  - поля формы сгруппированы, служебные скрыты;
  - подключена тема `django-unfold` (тёмная, современная, с цветными
    статусами) — согласовано с владельцем;
  - тема всегда тёмная (без белого светлого варианта), акцентный цвет
    приглушённый серо-синий вместо ярко-фиолетового;
  - ошибки в форме подсвечиваются на самом поле, введённый текст не
    стирается — сбрасываются только выбранные файлы фото (так себя
    ведёт браузер, это не чинится на стороне сайта, только предупреждён
    текстом);
  - поле «Внутренняя заметка» у автомобиля — видно только в админке;
  - файлы: `cars/admin.py`, `cars/models.py`, `config/settings.py`.

- [ ] **Доработка админки (со следующей сессии)** — *Ян*, продолжение
  `feature/admin-usability`
  - [x] Перетаскивание фото: было можно хватать только маленькую
    иконку — теперь можно и само фото, и область вокруг него; фото
    выложены в ряд (сеткой), а не колонкой.
  - [x] Баг: карточка «This item will be deleted. Click to cancel»
    (нажал корзину → предлагает отменить удаление) наезжала на
    соседние фото — вынес её из flex-раскладки карточки в отдельный
    оверлей поверх своей же карточки (`:has(.delete:checked)` в
    `admin-overrides.css`). Отмена проверена — карточка возвращается
    как была.
  - [x] Пропорции фото: было жёсткое `object-fit: cover` в
    альбомную рамку — вертикальные фото с телефона обрезались
    наполовину, нельзя было толком оценить фото при редактировании.
    Заменил на `object-fit: contain` (показывает фото целиком,
    с полосками по краям, если пропорции не альбомные) — это только
    для превью в админке, не влияет на будущую карточку в каталоге
    на сайте (там уже друг сам решит, cover или нет).
  - [x] Видео у автомобиля: одно видео на машину (не несколько, как
    фото), загружается файлом на наш сервер — владелец выбрал этот
    вариант, а не ссылку на YouTube. `Car.video`, mp4/mov/webm, до
    100 МБ (проверено — файлы больше лимита отклоняются). Показывается
    в админке (превью с плеером) и в заглушке страницы автомобиля.
  - [x] Предпросмотр карточки и публикация: владелец выбрал отдельное
    поле `Car.is_published` (не связано со «Статусом»). Новая машина
    по умолчанию черновик — не видна на сайте, пока не опубликована.
    Отдельная зелёная кнопка **«Опубликовать»** рядом с «Сохранить» —
    ставит `is_published=True` и сохраняет, не уходя со страницы.
    Обратное действие («Снять с публикации») — только массовым
    действием из списка машин, отдельной кнопки на самой странице для
    этого нет. Чекбокс в форме убрали по просьбе владельца (2026-09-03)
    — вместо него маленький статус-бейдж «ОПУБЛИКОВАНО»/«ЧЕРНОВИК»
    рядом с кнопками (не редактируется, только показывает состояние).
    **Важно:** поле `is_published` исключено из `CarAdminForm`
    (`exclude`, не просто убрано из fieldset) — иначе обычное
    «Сохранить» без явного действия тихо сбрасывало бы публикацию
    обратно в черновик, как раньше это было с фото.
    «Предпросмотром» стала стандартная кнопка Django-админки
    «Смотреть на сайте» (появилась сама благодаря
    `Car.get_absolute_url`) — перенесена вниз формы и вправо, подальше
    от «Удалить автомобиль» (случайно не перепутать). Проверено:
    обычный посетитель получает 404 на неопубликованную машину,
    залогиненный сотрудник (`is_staff`) видит её с пометкой
    «Черновик»; кнопка «Опубликовать» проверена от клика до записи в
    базу.
  - [x] Плюсик добавления объекта в сайдбаре рядом с названием раздела
    (`templates/admin/app_list.html`) — работает для всех разделов
    (Автомобили, Автосалон, Заявки), не только для машин; кнопка
    «+» в правом верхнем углу страницы осталась и продолжает
    работать, эта — просто более быстрый путь из самого меню.
    Демо-данные (`seed_demo`) публикуются сразу; 16 уже существующих
    машин в базе опубликованы отдельно, чтобы не пропали с сайта
    из-за нового поля.
    Сделано при слиянии с веткой друга 2026-09-03: `catalog()` теперь
    фильтрует `is_published=True`, черновики в списке не показываются.

## Этап 2 — страница автомобиля

- [x] **URL и данные страницы** — *Ян*, ветка `feature/car-detail`
  - маршрут `/cars/<slug>/`, view;
  - проданные и зарезервированные машины остаются доступны по ссылке
    (показывают статус), 404 только для несуществующих — чтобы старые
    ссылки из Instagram не ломались.
- [x] **Вёрстка страницы** — *Друг*, ветка `feature/car-detail-ui`
  (замёржена 2026-09-03) — галерея с лайтбоксом (`static/js/gallery.js`),
  хлебные крошки, блок характеристик, липкий блок с ценой и кнопками
  связи, закреплённая панель снизу на мобильном. Плюс `privacy`/`terms`
  (`cars/views.py`, маршруты в `cars/urls.py`) и общий футер
  `templates/partials/site_footer.html`.
  При слиянии Ян доклеил в чужую вёрстку два своих недавних
  добавления, которых на момент ветки друга ещё не было: пометку
  «Черновик» для неопубликованных машин и блок `<video>` (простой,
  без стилизации под общий дизайн — на потом).

## Передать другу для чипов и второй валюты (2026-09-03)

Друг упёрся в `models.py`/`admin.py` — его зона (`feature/car-detail-ui`),
чтобы туда не лезть, попросил три вещи у Яна:

- [ ] Новые поля (~15 строк, по словам друга):
  - `Car.accident` (bool) — «Был в ДТП»;
  - `Car.imported_from` (CharField, blank) — «Пригнан из» (свободный
    текст: «США», «Германия»...);
  - `Car.negotiable` (bool) — «Торг уместен»;
  - `Dealer.secondary_currency` (choices, те же валюты, что и
    основная) — вторая валюта для показа цены;
  - `Dealer.exchange_rate` (Decimal) — курс обмена, обновляется вручную
    в админке. Друг сознательно отказался от API НБУ по расписанию —
    для MVP это фоновые задачи, перебор; ручное поле — ~15 строк.
  - Миграция + показать в админке (fieldsets/список).
- [ ] `cars/admin.py:216` (цена в списке машин) — заменить на вызов
  `cars/formatting.py` (`format_price`), которую сделал друг, вместо
  своей копии той же логики — раз она теперь общая.
- [ ] Сказать другу, когда поля появятся — чипы «Был в ДТП» / «Пригнан
  из США» / «Торг уместен» и вторая валюта на карточке — это его
  примерно получасовая работа со своей стороны, ждёт полей.

## Этап 3 — фильтры и сортировка

- [ ] **Фильтрация** — *Ян*, ветка `feature/filters`
  - марка, модель, цена от/до, год от/до, пробег, топливо, коробка;
  - сортировка: новые / дешевле / дороже.
- [ ] **UI фильтров на мобильном** — *Друг*, ветка `feature/filters-ui`
  - компактная панель фильтров, не занимающая пол-экрана.

## Этап 4 — заявка на просмотр

- [ ] **Форма и сохранение** — *Ян*, ветка `feature/booking`
  - форма: имя, телефон, дата, время, комментарий;
  - сохранение `Booking`, раздел заявок в админке.
- [ ] **Вид формы и сообщение об успехе** — *Друг*, ветка
  `feature/booking-ui`

## Этап 5 — полировка

- [ ] Проверка всего сайта на телефоне, правки вёрстки —
  ветки `fix/...`
- [ ] Обновить README, скриншоты — любой.

## Идеи на будущее (после MVP, не делаем сейчас)

Telegram-уведомления о заявках, WhatsApp, счётчик просмотров,
избранное, сравнение, SEO-страницы, импорт машин, несколько
сотрудников/салонов, SaaS, свои домены.
